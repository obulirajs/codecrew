# CodeCrew — Project Memory

## What this is
Multi-agent DevOps orchestration platform. MS Teams is the sole user interface (chat-ops); the orchestrator routes requests to specialist agents that read JIRA, generate code, manage GitHub, and trigger CI/CD. Supervisor/orchestrator pattern built on LangGraph. This is a learning project (interview prep) with a couple of collaborators - optimize for clarity and consistency over cleverness.

## Source of truth for requirements
Requirements, acceptance criteria, and status live in **Jira project CDC** (obulirajs.atlassian.net), connected via GitHub for Atlassian. Always read the relevant ticket's AC before implementing a story. This file describes *how* we build, not *what* to build - don't duplicate ticket content here.

## Stack
Python, FastAPI, LangGraph, Anthropic Claude API (with a config-driven switch to local Ollama), MS Teams via Azure Bot Service (Bot Framework). pip + requirements.txt (not poetry/uv). Repo: github.com/obulirajs/codecrew (monorepo, backend/ is active; frontend/ reserved for future UI).

## Architecture (current)
```
adapters/       chat-platform in/out ONLY (Teams so far; Slack/Google Chat later - same NormalizedEvent contract)
clients/        external API wrappers (jira_client.py - JIRA REST v3, auth+retries+typed errors; jira_ticket_spec.py - pure data-shaping on top of it for the codegen agent; github_client.py - GitHub REST API, PAT auth, same retry/error pattern as jira_client.py)
codegen/        Epic 2 codegen agent internals (workspace.py - git worktree lifecycle per ticket off one canonical clone; agent.py - headless invocation via the Claude Agent SDK, not a raw Messages API call - Epic 2's architecture decision, since codegen needs to explore the real repo before writing code)
orchestrator/   LangGraph state, nodes, graph wiring - one node per agent capability
llm_client.py   single entry point for all LLM calls (provider switch lives here only)
main.py         FastAPI routes
config.py       pydantic-settings, fails fast on missing secrets
```

## Conventions (non-negotiable - match these, don't reinvent)
- **Config**: all secrets/settings go through `app/config.py` (pydantic-settings). Never read `os.environ` elsewhere.
- **Logging**: structured JSON via `app/logging_config.py`. One logger per module: `codecrew.<module_name>`.
- **adapters/ vs clients/**: adapters are chat platforms only. clients are external system APIs (Jira, GitHub, CI). Don't mix these - it's what keeps the chat layer swappable.
- **clients/ vs codegen/**: `clients/` holds thin, reusable wrappers around an external system's REST API (auth, retries, typed errors) - Jira today, GitHub/CI later. The Claude Agent SDK code in `codegen/` is NOT that: `query()` spawns a whole subprocess-based coding agent with its own tool loop (file edits, bash, repo exploration), not a REST call being wrapped. Think of `codegen/`'s three files (workspace.py, agent.py, diff.py) as a capability module - a peer of `orchestrator/jira_agent.py`, not of `clients/jira_client.py` - that just isn't wired into the LangGraph graph as a node yet. This is also why `codegen/agent.py` is deliberately exempt from the `llm_client.py` rule below - it's a headless agent process, not a raw completion call.
- **LLM calls**: always go through `app/llm_client.py`'s `chat_completion()`. Never instantiate an Anthropic or Ollama client anywhere else. Provider chosen via `LLM_PROVIDER` env var (`anthropic` | `ollama`); cheap/strong model tiers resolved via `Settings.cheap_model` / `Settings.strong_model`.
- **Prompt caching**: `chat_completion()` applies Anthropic prompt caching (`cache_control`) to system prompts over 500 characters - shorter ones pass through as a plain string, uncached. Cache hit/write token counts (`cache_creation_input_tokens` / `cache_read_input_tokens`) are logged at debug level on every Anthropic call for verification.
- **LangGraph**: one node per agent/capability. Routing is via conditional edges keyed on `state["intent"]`, not if/else chains in a single node.
- **Blocking SDKs** (e.g. `botframework-connector`, which is sync): wrap in `asyncio.to_thread` so the FastAPI event loop isn't stalled.
- **Bot Framework activities**: filter on `payload["type"] == "message"` before processing - Teams/Web Chat send `typing` events too.
- **Pinned `starlette`/`uvicorn`**: `requirements.txt` pins both below what `claude-agent-sdk`'s own `mcp` dependency wants (it asks for `starlette>=0.48.0`/`uvicorn>=0.31.1`; we're on `starlette==0.38.6`/`uvicorn==0.30.6` for fastapi 0.115.0 compatibility). This works today but is a known latent conflict - see CDC-47. Don't casually bump either version without checking that ticket first.

## Status (update after each story - keep this section short, just epic/story + state)
- Bug (CDC-53): fixed - `app/llm_client.py`'s `chat_completion()` blindly indexed `response.content[0].text`, assuming the text block always comes first. Discovered via CDC-30's review agent call (a larger, more reasoning-heavy prompt than the simple intent-classifier/JIRA-agent calls that have used this function since Epic 0) triggering an extended-thinking block ahead of the text block, raising `AttributeError` - a latent bug in shared code every LLM call in the project goes through, not scoped to review. Fixed with a new `_extract_text()` that scans `response.content` for the first block with `type == "text"`; a response with no text block at all now raises a specific `LLMResponseError` instead of an unrelated `AttributeError`. Entirely internal to `chat_completion()` - no caller (`intent_classifier.py`, `jira_agent.py`, `pr_review.py`) needed to change.
- **Epic 0** (CDC-5): Done. Teams <-> FastAPI <-> LangGraph round trip verified against real Teams (not just Web Chat). GitHub for Atlassian connected (commits/PRs auto-link to tickets; Smart Commits enabled).
  - 0.7 Local rotating file logging (CDC-18): done
  - 0.8 Request correlation ID across all log lines (CDC-19): done
- **Epic 1** (CDC-11): In progress.
  - 1.1 Show ticket summary on request (CDC-12): done
  - 1.2 List tickets assigned to me (CDC-13): done
  - 1.3 JIRA client wrapper (CDC-14): done
  - 1.4 Extract structured acceptance criteria (CDC-15): done
  - 1.5 Unit tests with mocked JIRA API (CDC-16): done
- **Epic 2** (CDC-40): In progress.
  - 2.1 Generate code diff for a single ticket (CDC-41): done - `app/codegen/diff.py`'s `generate_diff()` feeds a CDC-15 `TicketSpec` to the headless agent, lets it edit real files in a worktree, then captures the actual `git diff` (not model free text) as `{diff_text, summary, files_changed}`. Stops short of commit/push/PR (Epic 3). Extended `ticket_workspace()` with `cleanup_on_success` so a successful run keeps its worktree alive for Epic 3 to commit from later; failure still always cleans up.
  - 2.3 Flag ambiguous requirements instead of guessing (CDC-43): done - `generate_diff()`'s prompt now tells the agent to leave files untouched and end its response with a delimited `<<<NEEDS_CLARIFICATION>>>...<<<END_NEEDS_CLARIFICATION>>>` block (parsed via regex, not substring matching) when acceptance criteria are vague, contradictory, or impossible as written. When present, `CodegenResult` comes back with `needs_clarification=True` and `clarifying_questions`, diff/files empty, and the worktree is cleaned up immediately (nothing for Epic 3 to commit). If the agent makes no changes and never raises the marker, that's now treated as an unexpected failure (`CodegenError`, logged), not a silent empty success.
  - 2.5 Lint/validate generated code before handoff to git agent (CDC-45): done - after CDC-41's diff capture, `generate_diff()` runs ruff (new dependency) against the `.py` files in `files_changed` (`--select=E9,F` - syntax errors + pyflakes only, not ruff's full style rule set; non-`.py` files skipped) and adds `lint_errors: list[str]` to `CodegenResult`. Non-empty `lint_errors` still returns a populated `diff_text`/`files_changed` and keeps the worktree (same as a clean run) - real code exists, just flawed - but signals Epic 3 must check `lint_errors` before committing. Clean runs are unchanged from CDC-41/CDC-43.
  - 2.2 Codegen works against a real repo checkout via git worktree (CDC-42): done
  - 2.7 Prefer CLAUDE_CODE_OAUTH_TOKEN over API key when available (CDC-50): done
  - 2.8 Scheduled cleanup of stale codegen worktrees past retention window (CDC-52): done - new `WORKTREE_RETENTION_HOURS` setting (default 48). `workspace.py` gained `list_ticket_worktrees()` (every per-ticket worktree off the canonical clone, excluding the clone's own "main" entry and any detached one) and `delete_ticket_branch()`. New standalone `scripts/sweep_stale_worktrees.py` - not a scheduler, run periodically via cron/Task Scheduler or by hand - ages each worktree by filesystem mtime (git tracks no creation time) and, once older than the retention window, removes it via the existing `remove_worktree()` (`--force`) then deletes its branch. No-op run logs a short summary line; verified end-to-end against a real repo, not just mocks.
  - Bug (CDC-51): fixed - re-running a ticket after its worktree was cleaned up falsely raised WorktreeInProgressError, because the duplicate check looked at branch existence, not actual worktree checkout state (`git worktree remove` leaves the branch behind). `create_worktree()` +
- **Epic 3**: In progress.
  - 3.6 GitHub API client wrapper (CDC-28): done - `app/clients/github_client.py`'s `GitHubClient` (PAT auth, not a GitHub App - registration/JWT overhead not justified at this scale) covers `create_pull_request()`, `list_pull_requests(state, assignee)` (assignee filtering goes through the Search API since `/pulls` has no assignee filter of its own), and `get_pull_request()`. Same retry-once-on-5xx/timeout pattern as `jira_client.py`; backs off on `Retry-After` for GitHub's secondary rate limits (403/429 with that header - checked before the generic 401/403 auth-failure path, so a rate-limited 403 isn't misreported as an auth failure). Distinct exceptions for 404/401-403/422 - 422 (`GitHubValidationError`) is the real-world "PR already open for this branch" case Epic 3's commit/PR step must handle, not crash on. New required `GITHUB_TOKEN`/`GITHUB_OWNER`/`GITHUB_REPO` settings. `scripts/github_smoke_test.py` confirmed real auth against obulirajs/codecrew end to end (0 open PRs, as expected).
  - 3.2 Commit generated code with ticket-referencing message (CDC-24): done - new `app/codegen/commit.py`'s `commit_generated_code(ticket_key, result)` is the actual enforcement point for CDC-45's "flawed diffs must not be committed" rule: refuses up front (no git touched) if `result.lint_errors` is non-empty or `result.needs_clarification` is True, with a clear precondition error either way. Stages exactly `result.files_changed` via `git add --` (never `-A`), commits with author identity set explicitly per-commit via `git -c user.name=... -c user.email=...` (new `GIT_COMMIT_AUTHOR_NAME`/`_EMAIL` settings, default "CodeCrew Bot" / "noreply@codecrew.local" - no dependence on global git config), and a `"<TICKET-KEY> <summary>"` message matching this project's existing Smart Commit convention, reusing `CodegenResult.summary` rather than a fresh LLM call. Worktree lookup by ticket key alone via new `workspace.list_ticket_worktrees()`-backed `find_ticket_worktree()`. Worktree is left in place on success, ready for CDC-23 to push. Verified end-to-end against a real repo: correct bot identity, correct message, and a stray untracked file in the worktree was confirmed NOT swept into the commit.
  - 3.1 Push branch to GitHub remote (CDC-23): done - despite the numbering, this is actually the last step after CDC-24 (the local branch itself was already created by CDC-42 at worktree creation time; scope here is specifically pushing it). New `app/codegen/push.py`'s `push_ticket_branch(ticket_key)` pushes via an explicit HTTPS URL with `GITHUB_TOKEN` embedded (`https://x-access-token:<token>@github.com/...`), not the worktree's ambient `origin` remote, so it authenticates correctly regardless of the host machine's own git credential setup. The token is redacted (`***`) from every log line and every raised error message - verified directly, including the case where git's own stderr echoes the full URL back. Never force-pushes: a rejected (non-fast-forward) push raises `PushError` immediately with no retry; any other failure retries once, matching this project's existing pattern. Verified against a real local bare repo (not the real GitHub remote): a real push landed the branch+commit, and a real induced non-fast-forward rejection was caught and confirmed to leave the remote's history untouched.
  - 3.4 List open PRs / PRs assigned to me (CDC-26): done - new `app/codegen/pr_list.py` (composition layer alongside `pr.py`, not added to `github_client.py` itself) with `list_open_pull_requests()` and `list_my_pull_requests(state="open")`, both returning `List[PullRequestSummary]{number, title, state, draft, url}` - an empty list, not an error, when nothing matches. Needed only one new REST capability: `github_client.py`'s `get_authenticated_user()` (`GET /user`), used to resolve "me" - there's no real per-user identity mapping yet (same simplification as CDC-13's JIRA identity gap, an Epic 8 concern), so "me" is whichever account `GITHUB_TOKEN` itself belongs to. "Assigned to me" specifically means GitHub's actual assignees field (distinct from author/requested-reviewers) - already reachable via `list_pull_requests(assignee=...)`'s existing Search API path from CDC-28, so no new filtering logic was needed there. Returns plain function results - no orchestrator/Teams wiring yet (same note as CDC-25/CDC-30).
  - 3.3 Open PR with auto-filled description linked to ticket (CDC-25): done - new `app/codegen/pr.py`'s `open_pull_request(ticket_key, spec, result)` composes CDC-28's `GitHubClient` with local ticket data (business logic lives here, not in `github_client.py`, which stays a pure REST wrapper). Title is `"<TICKET-KEY>: <summary>"` (alone enough for GitHub for Atlassian's Development-panel auto-link, same mechanism as commits - no separate Jira call); body combines `TicketSpec`'s summary/acceptance_criteria with `CodegenResult.summary` plus a direct Jira ticket link. `head` reuses `workspace.branch_name()` (not re-derived); `base` comes from `github_client.py`'s new `get_repository()` (real default branch, not hardcoded "main" - verified against the real repo, which returned `"main"`). Also extended `list_pull_requests()` with an optional `head` filter (owner-prefixing handled inside the client). On a 422 (`GitHubValidationError` - a PR already open for this branch), looks the existing PR up via `list_pull_requests(head=...)` and returns it instead of raising, making a re-run idempotent; a defensive `PullRequestError` covers the case where the 422 fires but no match is actually found. Returns `PullRequestResult{number, url}` - no orchestrator/Teams wiring exists yet, so nothing consumes this beyond the function boundary. New `scripts/pr_smoke_test.py` (hand-built `TicketSpec`/`CodegenResult`, same pattern as `commit_smoke_test.py`) confirmed `open_pull_request()` end to end against the real repo: opened PR #1 (`feature/TEST-8-add-a-comment-to-readme-md` -> `main`).
- **Epic 4** (CDC-29): In progress.
  - 4.2 Structured review output (verdict, risk_score, flagged_files) (CDC-31): done - built first, ahead of CDC-30 (4.1), since it defines the contract CDC-30 consumes (same reasoning as CDC-42 before CDC-41). New `app/review/pr_review.py`'s `review_pull_request(pr_number)` fetches the PR's changed files/patches via `github_client.py`'s new `get_pull_request_files()` (`pulls/{pr}/files`, single page up to 100 - same no-pagination simplicity as `list_pull_requests()`), builds a diff-text prompt (files GitHub omits a patch for - binary or oversized - get a placeholder note rather than being silently dropped), and asks the strong-tier model via `chat_completion()` (not the Agent SDK - per the ticket's architecture decision, reviewing a diff doesn't need repo exploration) for a single JSON object matching `ReviewResult{verdict, risk_score, flagged_files, file_comments, summary}`. Parsed defensively (tolerant of a stray markdown code fence around the JSON) into a pydantic model that itself constrains `verdict` to `"approve"|"request_changes"` and `risk_score` to `[0.0, 1.0]` - any parse or validation failure raises `ReviewParsingError`, which is never swallowed into a default "approve" (the ticket's core safety requirement). Stops at producing `ReviewResult` - doesn't post anything to GitHub (CDC-30's job) or decide auto-merge eligibility (CDC-32's job).
  - 4.3 Deterministic auto-merge rules engine (CDC-32) + 4.4 Auto-merge off by default (CDC-33): done - built together per the ticket (33 is the off-by-default guarantee/verification companion to 32's engine, not separable). New `app/review/merge_rules.py`'s `evaluate_merge_eligibility(review, ci_status, ticket_type, changed_files, diff_lines)` is a pure function (no GitHub calls) returning `MergeDecision{allowed, reasons}` - `reasons` carries one PASS/FAIL entry per rule, whether blocked or allowed, not just failures. Rules: `review.verdict == "approve"` (CDC-31's internal `ReviewResult`, never GitHub's own review-decision state, since CDC-30's self-review restriction means every review posts as a plain COMMENT - the function takes no GitHub review-decision param at all, so that mistake isn't representable); `ci_status == "success"` exactly (new `MAX_AUTO_MERGE_LINES`-adjacent settings; `None`/anything else always fails - there's no CI pipeline yet (Epic 5), so this correctly makes auto-merge impossible today, which is intended, not a bug); diff size under new `MAX_AUTO_MERGE_LINES` (default 200); no changed file matching new `AUTO_MERGE_SENSITIVE_PATHS` (default `config.py`, `.env*`, `clients/*` - matched at any path depth via `_matches_sensitive_path()`'s per-suffix `fnmatch`); `ticket_type` in new `AUTO_MERGE_ALLOWED_TICKET_TYPES` (default Chore/Docs/Task). New `app/review/auto_merge.py`'s `execute_auto_merge(pr_number, decision)` is the separate, actually-risky execution function - it independently re-checks both `settings.auto_merge_enabled` (new flag, default `False` - CDC-33's core guarantee) and `decision.allowed` itself rather than trusting the caller, so it's never reachable with auto-merge disabled regardless of how many rules passed; only then does it call `github_client.py`'s new `merge_pull_request()` (`pulls/{pr}/merge`) and log which rule(s) permitted it. Not run against the real repo - a real merge is far harder to reverse than a push/PR/comment; the decision function (no GitHub calls) is fully unit-tested on its own.
  - 4.5 Audit log of auto-merge decisions with rule trace (CDC-34): done - CDC-32 already logged `decision.reasons` via `execute_auto_merge()`; this story's real gap was that neither `evaluate_merge_eligibility()` nor `execute_auto_merge()` took or logged `ticket_key`, so a decision couldn't be tied back to its originating Jira ticket. Both functions now take `ticket_key` (and `evaluate_merge_eligibility()` also gained `pr_number`, needed purely so its own audit line can carry it too). Both now log through a new, distinct `codecrew.audit` logger (separate from general app logs, independently filterable/greppable even before Epic 8 story 8.3's fuller queryable audit store) - `evaluate_merge_eligibility()` logs one "decision computed" line (`executed=False` always, since evaluating never merges), and `execute_auto_merge()` logs one line per actual outcome (blocked by config / blocked by rules / executed), each carrying `ticket_key`, `pr_number`, an explicit `executed` bool, and the full `reasons` trace regardless of outcome. No database/dedicated store added - out of scope per the ticket. Tests use `caplog` to confirm a decision for a given `ticket_key` is findable/filterable from captured log output. New `scripts/audit_log_smoke_test.py` (a fake `allowed=False` `MergeDecision`, inherently safe since that guarantees no GitHub call regardless of config) confirmed the real log line end to end.
  - 4.1 Review PR and return verdict plus risk summary (CDC-30): done - new `app/review/post_review.py`'s `post_pr_review(pr_number)` calls CDC-31's `review_pull_request()` for the `ReviewResult`, then posts it as a real GitHub PR review via `github_client.py`'s new `create_pull_request_review()` (`pulls/{pr}/reviews`), `file_comments` mapped to GitHub's inline comment schema (`{path, line, body}`). Corrected understanding after two rounds of live-repo testing: GitHub's self-review restriction blocks BOTH `APPROVE` and `REQUEST_CHANGES` from a PR's own author (`GITHUB_TOKEN` always authors the PRs it would review, per CDC-25) - only a plain `COMMENT` event succeeds regardless of verdict, so every review this posts is now `COMMENT`; `_review_body()` puts the actual recommended verdict ("Recommended: Approve"/"Recommended: Request Changes"), `risk_score`, `flagged_files`, and per-file comments into the body text, since none of that is conveyed by GitHub's native review-state UI when using `COMMENT`. That same testing exposed a real gap in `github_client.py`'s error-detail extraction: GitHub's `errors` array shows up as either `{message: ...}` objects (PR-creation 422s) or plain strings (review 422s, e.g. `"Review Can not request changes on your own pull request"`) - the first fix only handled the dict shape and silently missed the string shape, which is exactly what hid the real error during this story's testing; `_error_detail()` now checks each entry's type and extracts either shape. Returns `PostedReview{verdict, risk_score, url}` as a plain function result - no orchestrator/Teams wiring yet, same note as CDC-25. Posts a real, visible action to GitHub (same caution category as CDC-23/CDC-25) - not run against the real repo without explicit go-ahead. Explicitly out of scope: auto-merge decision/execution (CDC-32/33's job).